# Server Port
PORT=8080

# Passkey Authentication Service (Frontend needs VITE_ prefix)
VITE_PASSKEY_API_URL=
PASSKEY_API_URL=

# API URL for frontend
VITE_API_URL=http://localhost:8080

# Your domain (for production)
VITE_APP_DOMAIN=

# Database Path (optional, defaults to server/db/pixelmap.db)
DB_PATH=./data/pixelmap.db

# Node Environment
NODE_ENV=production